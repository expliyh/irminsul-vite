image: node:20
pages:
#   cache:
#     paths:
#       - node_modules/
  script:
    # - apk add git # Uncomment this if you're using small docker images like alpine and have lastUpdated enabled
    - npm install
    - npm run docs:build
    - sshpass -p $SSH_PASS scp -r public/* autoupload@zjk.irminsul.top:/opt/1panel/apps/openresty/openresty/www/sites/www.irminsul.top/index/
#   artifacts:
#     paths:
#       - public
  only:
    - main